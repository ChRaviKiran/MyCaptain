from math import pi                                                       # imports the value of pi 
r = float(input("Input the radius of the circle : "))                     # r : Radius
print("Area of the circle with radius "+str(r)+" is : "+str(pi*r*r))      # prints area of the cirlce
